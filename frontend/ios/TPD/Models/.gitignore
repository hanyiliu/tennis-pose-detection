# Core ML bundles are build artifacts (~34 MB fp16), regenerated by
# frontend/ios/tools/export_coreml.py from the checkpoint in exports/.
# The JSON sidecars next to them ARE committed -- they are the single source of
# truth Swift reads for labels, input sizes and class count.
*.mlpackage/
*.mlmodelc/
