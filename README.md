# Tennis Pose Detection

Given an image of a tennis player, we propose the model `TPD` which will be able to classify the current pose of the player into one of four classes: `backhand`, `forehand`, `ready_position`, or `serve`.
